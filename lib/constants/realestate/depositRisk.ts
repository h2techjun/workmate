/**
 * 깡통전세 위험도 판정 상수.
 *
 * ⚠️ 아래 임계값은 "업계·언론·HUG가 통상 인용하는 경험칙"이며 법정 기준이 아니다.
 *    실제 HUG/SGI 전세보증보험 가입 요건·한도는 수시 변경되므로 UI에서 반드시
 *    "직접 확인" 안내를 병기한다. (과장·확정 표현 금지)
 *
 * 근거·출처:
 *  - 부채비율(선순위 채권 + 임차보증금) ÷ 주택가격: HUG 전세보증금반환보증 심사에서
 *    널리 쓰이는 안전성 지표. 통상 80~90% 이하를 상대적 안전선으로 인용.
 *  - 전세가율(보증금 ÷ 매매시세) 70~80% 초과 = "깡통전세" 위험 신호 (부동산원·언론 통용).
 *  - 경매 낙찰가율: 법원경매 물건별로 크게 상이(대략 70~90%). 고정값이 아니므로
 *    사용자가 조정하는 "가정치"로 두고 기본값만 보수적으로 제시한다.
 */

/** 부채비율/전세가율 위험 구간 경계 (통상 경험칙, 법정 아님) */
export const RATIO_SAFE_MAX = 0.7; // 이하 = 상대적 안전
export const RATIO_CAUTION_MAX = 0.8; // 초과~0.8 = 주의
export const RATIO_DANGER_MAX = 0.9; // 초과~0.9 = 위험, 초과 = 매우 위험

/** 경매 낙찰가율 기본 가정치 (%). 물건별 상이 — 사용자 조정 대상. */
export const DEFAULT_AUCTION_RECOVERY_PERCENT = 80;

export type RiskLevel = "safe" | "caution" | "danger" | "critical";
