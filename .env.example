# Workmate 환경변수
# 실제 사용 시 .env.local 파일로 복사 후 값 채워서 사용 (.gitignore 처리됨)

# 사이트 절대 URL (sitemap·robots·schema·OG 메타데이터에 사용)
NEXT_PUBLIC_SITE_URL=https://workmate.tools

# Vercel 배포 시 별도 설정 불필요 — 대시보드의 Environment Variables 에서
# 같은 키로 추가하면 production 빌드에 자동 반영.
